<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Truyện Hay - Chi tiết truyện</title>
    <!-- Font Awesome 6 -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:opsz,wght@14..32,400;14..32,500;14..32,600;14..32,700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="~/css/layout.css" asp-append-version="true">
    
</head>
<body>
    <div class="app">
        <!-- HEADER -->
        <header class="header">
            <div class="logo-area">
                <button class="mobile-menu-btn" id="mobileMenuBtn"><i class="fas fa-bars"></i></button>
                <a href="#" class="logo" id="logo">WIBU<span>HUB</span></a>
            </div>
            <div class="search-bar">
                <input type="text" placeholder="Tìm truyện, tác giả..." id="searchInput">
                <button id="searchBtn"><i class="fas fa-search"></i></button>
            </div>
            <div class="user-actions">
                <a href="#" id="loginBtn"><i class="far fa-user"></i> <span>Đăng nhập</span></a>
                <a href="#" id="notificationBtn"><i class="far fa-bell"></i> <span>Thông báo</span></a>
                <div class="avatar" id="avatarBtn">U</div>
            </div>
        </header>

        <!-- MOBILE MENU OVERLAY & SIDEBAR -->
        <div class="mobile-menu-overlay" id="mobileMenuOverlay"></div>
        <div class="mobile-menu" id="mobileMenu">
            <div class="mobile-menu-header">
                <span class="mobile-menu-logo">TRUYỆN<span>HAY</span></span>
                <span class="close-menu" id="closeMenu"><i class="fas fa-times"></i></span>
            </div>
            <a href="#" class="mobile-menu-item active" id="mobileHome"><i class="fas fa-home"></i> Trang chủ</a>
            <a href="#" class="mobile-menu-item" id="mobileNew"><i class="fas fa-history"></i> Mới cập nhật</a>
            <a href="#" class="mobile-menu-item" id="mobileHot"><i class="fas fa-fire"></i> Hot nhất</a>
            <a href="#" class="mobile-menu-item" id="mobileGenre"><i class="fas fa-tags"></i> Thể loại</a>
            <a href="#" class="mobile-menu-item" id="mobileRank"><i class="fas fa-crown"></i> BXH</a>
            <a href="#" class="mobile-menu-item" id="mobileCompleted"><i class="fas fa-check-circle"></i> Hoàn thành</a>
            <a href="#" class="mobile-menu-item" id="mobileGroups"><i class="fas fa-users"></i> Nhóm dịch</a>
            <a href="#" class="mobile-menu-item" id="mobileProfilePage"><i class="fas fa-user"></i> Cá nhân</a>
        </div>

        <!-- MODAL ĐĂNG NHẬP / ĐĂNG KÝ -->
        <div id="authModal" class="modal">
            <div class="modal-content">
                <span class="modal-close" id="closeModal">&times;</span>
                <div class="modal-title" id="modalTitle">Đăng nhập</div>
                <!-- Form đăng nhập -->
                <div id="loginForm">
                    <div class="input-group">
                        <label>Email</label>
                        <input type="email" id="loginEmail" placeholder="your@email.com">
                    </div>
                    <div class="input-group">
                        <label>Mật khẩu</label>
                        <input type="password" id="loginPassword" placeholder="••••••••">
                    </div>
                    <div class="captcha-box">
                        <span class="captcha-text" id="captchaText">A1B2C</span>
                        <span class="captcha-refresh" id="refreshCaptcha"><i class="fas fa-sync-alt"></i></span>
                    </div>
                    <div class="input-group">
                        <label>Nhập mã captcha</label>
                        <input type="text" id="captchaInput" placeholder="Nhập mã ở trên">
                    </div>
                    <button class="btn" id="doLogin">Đăng nhập</button>
                    <div class="switch-link">
                        Chưa có tài khoản? <span id="showRegister">Đăng ký ngay</span>
                    </div>
                </div>
                <!-- Form đăng ký -->
                <div id="registerForm" style="display: none;">
                    <div class="input-group">
                        <label>Email</label>
                        <input type="email" id="regEmail" placeholder="your@email.com">
                    </div>
                    <div class="input-group">
                        <label>Mật khẩu</label>
                        <input type="password" id="regPassword" placeholder="••••••••">
                    </div>
                    <div class="input-group">
                        <label>Xác nhận mật khẩu</label>
                        <input type="password" id="regConfirmPassword" placeholder="••••••••">
                    </div>
                    <div class="captcha-box">
                        <span class="captcha-text" id="regCaptchaText">XYZ99</span>
                        <span class="captcha-refresh" id="refreshRegCaptcha"><i class="fas fa-sync-alt"></i></span>
                    </div>
                    <div class="input-group">
                        <label>Nhập mã captcha</label>
                        <input type="text" id="regCaptchaInput" placeholder="Nhập mã ở trên">
                    </div>
                    <button class="btn" id="doRegister">Đăng ký</button>
                    <div class="switch-link">
                        Đã có tài khoản? <span id="showLogin">Đăng nhập</span>
                    </div>
                </div>
                <!-- Thông báo xác nhận email -->
                <div id="confirmEmailMessage" style="display: none; text-align: center;">
                    <i class="fas fa-envelope" style="font-size: 48px; color: #2563eb; margin: 20px;"></i>
                    <p style="margin: 16px 0;">Vui lòng kiểm tra email <strong>user@example.com</strong> để xác nhận tài khoản.</p>
                    <button class="btn btn-secondary" id="backToLogin">Quay lại đăng nhập</button>
                </div>
            </div>
        </div>

        <!-- MODAL PROFILE (quick view) -->
        <div id="profileModal" class="modal">
            <div class="modal-content large">
                <span class="modal-close" id="closeProfileModal">&times;</span>
                <div class="modal-title">Thông tin cá nhân</div>
                <!-- Tabs -->
                <div class="profile-tabs">
                    <div class="profile-tab active" data-tab="info">Thông tin</div>
                    <div class="profile-tab" data-tab="history">Lịch sử đọc</div>
                    <div class="profile-tab" data-tab="follow">Theo dõi</div>
                </div>
                <!-- Pane: Thông tin -->
                <div class="profile-pane active" id="pane-info">
                    <div style="display: flex; gap: 20px; align-items: center; margin-bottom: 24px;">
                        <div style="width: 80px; height: 80px; background: #2563eb; border-radius: 50%; display: flex; align-items: center; justify-content: center; color: white; font-size: 32px;">U</div>
                        <div>
                            <h3 style="font-size: 24px;">Nguyễn Văn A</h3>
                            <p style="color: #475569;"><i class="far fa-envelope"></i> user@example.com</p>
                            <p style="color: #475569;"><i class="fas fa-coins"></i> 1.250 Xu</p>
                        </div>
                    </div>
                    <button class="btn btn-secondary" style="width: auto; padding: 10px 24px;" id="logoutBtn">Đăng xuất</button>
                </div>
                <!-- Pane: Lịch sử đọc -->
                <div class="profile-pane" id="pane-history">
                    <div class="history-item"><div class="history-cover">Ảnh</div><div class="history-info"><div class="history-title">Đại chiến thần</div><div class="history-meta"><span><i class="far fa-clock"></i> Đọc gần đây: Chương 126</span><span>2 giờ trước</span></div></div></div>
                    <div class="history-item"><div class="history-cover">Ảnh</div><div class="history-info"><div class="history-title">Solo Leveling</div><div class="history-meta"><span><i class="far fa-clock"></i> Đọc gần đây: Chương 200</span><span>1 ngày trước</span></div></div></div>
                </div>
                <!-- Pane: Theo dõi -->
                <div class="profile-pane" id="pane-follow">
                    <div class="follow-item"><div class="follow-cover">Ảnh</div><div class="follow-info"><div class="follow-title">Đấu phá thương khung</div><div class="follow-meta"><span><i class="fas fa-book-open"></i> 1560 chương</span><span><i class="fas fa-eye"></i> 36.5k</span></div></div><button class="unfollow-btn" onclick="alert('Đã bỏ theo dõi (mô phỏng)')">Bỏ theo dõi</button></div>
                    <div class="follow-item"><div class="follow-cover">Ảnh</div><div class="follow-info"><div class="follow-title">Vũ đế cuồng thần</div><div class="follow-meta"><span><i class="fas fa-book-open"></i> 888 chương</span><span><i class="fas fa-eye"></i> 12.2k</span></div></div><button class="unfollow-btn" onclick="alert('Đã bỏ theo dõi (mô phỏng)')">Bỏ theo dõi</button></div>
                </div>
            </div>
        </div>

        <!-- Navigation tabs (desktop) -->
        <nav class="nav-tabs">
            <a class="nav-item" id="tabHome">Trang chủ</a>
            <a class="nav-item" id="tabNew">Mới cập nhật</a>
            <a class="nav-item" id="tabHot">Hot nhất</a>
            <a class="nav-item" id="tabGenre">Thể loại</a>
            <a class="nav-item" id="tabRank">BXH</a>
            <a class="nav-item" id="tabCompleted">Hoàn thành</a>
            <a class="nav-item" id="tabGroups">Nhóm dịch</a>
            <a class="nav-item" id="tabProfilePage">Cá nhân</a>
        </nav>

        <!-- Main grid -->
        <div class="main-grid">
            <!-- Left column -->
            <div>
                <!-- Content sections -->
                <!-- TRANG CHỦ (có featured, update list, recommend, top views) -->
                <div id="homeSection" class="content-section">
                    <!-- Featured large -->
                    <div class="featured-large">
                        <div class="featured-large-content">
                            <h3>NỔI BẬT</h3>
                            <h2>Shadow Warrior</h2>
                            <p>In a world where darkness threatens to consume everything, one warrior stands against the shadows. Follow the journey of Kai as he battles ancient evil forces.</p>
                            <div class="featured-stats">
                                <span><i class="fas fa-star" style="color: #fbbf24;"></i> 4.8</span>
                                <span><i class="fas fa-eye"></i> 1250K lượt xem</span>
                                <span><i class="fas fa-book-open"></i> 4 chương</span>
                            </div>
                            <button class="btn" style="width: auto; padding: 12px 32px;" id="readFeaturedBtn" data-id="shadow-warrior" data-title="Shadow Warrior" data-author="Tác giả D" data-genre="Hành động, Phiêu lưu" data-views="1250K" data-rating="4.8" data-votes="1250">Đọc ngay</button>
                        </div>
                        <div class="featured-large-cover">Ảnh bìa</div>
                    </div>

                    <!-- Grid 2 cột: update list và top views -->
                    <div class="home-grid">
                        <!-- Left column -->
                        <div>
                            <!-- Truyện mới cập nhật dạng list -->
                            <div class="update-list">
                                <div class="section-header">
                                    <h2>Truyện Mới Cập Nhật</h2>
                                    <span class="view-all-link" id="viewAllUpdates">Xem tất cả</span>
                                </div>
                                <div class="update-item" data-id="steel-resolve" data-title="Steel Resolve" data-author="Tác giả A" data-genre="Hành động, Phiêu lưu" data-views="1680K" data-rating="4.6" data-votes="1250">
                                    <div class="update-cover"></div>
                                    <div class="update-info">
                                        <div class="update-title">Steel Resolve</div>
                                        <div class="update-chapter">Chapter 1 • 01-12</div>
                                        <div class="update-meta">
                                            <span><i class="fas fa-star" style="color: #fbbf24;"></i> 4.6</span>
                                            <span><i class="fas fa-eye"></i> 1680K</span>
                                        </div>
                                    </div>
                                </div>
                                <div class="update-item" data-id="cosmic-legends" data-title="Cosmic Legends" data-author="Tác giả B" data-genre="Khoa huyễn, Viễn tưởng" data-views="2100K" data-rating="4.9" data-votes="2100">
                                    <div class="update-cover"></div>
                                    <div class="update-info">
                                        <div class="update-title">Cosmic Legends</div>
                                        <div class="update-chapter">Chapter 2 • 08-11</div>
                                        <div class="update-chapter">Chapter 1 • 01-11</div>
                                        <div class="update-meta">
                                            <span><i class="fas fa-star" style="color: #fbbf24;"></i> 4.9</span>
                                            <span><i class="fas fa-eye"></i> 2100K</span>
                                        </div>
                                    </div>
                                </div>
                                <div class="update-item" data-id="dragon-quest" data-title="Dragon Quest" data-author="Tác giả C" data-genre="Kỳ ảo, Phiêu lưu" data-views="1450K" data-rating="4.7" data-votes="980">
                                    <div class="update-cover"></div>
                                    <div class="update-info">
                                        <div class="update-title">Dragon Quest</div>
                                        <div class="update-chapter">Chapter 5 • 15-12</div>
                                        <div class="update-meta">
                                            <span><i class="fas fa-star" style="color: #fbbf24;"></i> 4.7</span>
                                            <span><i class="fas fa-eye"></i> 1450K</span>
                                        </div>
                                    </div>
                                </div>
                            </div>

                            <!-- Truyện đề cử với tabs -->
                            <div>
                                <div class="section-header">
                                    <h2>Truyện Đề Cử</h2>
                                    <div class="recommend-tabs">
                                        <span class="recommend-tab active" data-recommend="all">Tất cả</span>
                                        <span class="recommend-tab" data-recommend="popular">Phổ biến</span>
                                        <span class="recommend-tab" data-recommend="latest">Mới nhất</span>
                                    </div>
                                </div>
                                <div class="recommend-grid" id="recommendAll">
                                    <div class="recommend-card" data-title="Dạng ra 1" data-rating="4.5" data-votes="1300">
                                        <div class="recommend-cover"></div>
                                        <div class="recommend-info">
                                            <div class="recommend-title">Dạng ra</div>
                                            <div class="recommend-stats">
                                                <span>4 chương</span>
                                                <span>1.3M</span>
                                            </div>
                                        </div>
                                    </div>
                                    <div class="recommend-card" data-title="Dạng ra 2" data-rating="4.2" data-votes="980">
                                        <div class="recommend-cover"></div>
                                        <div class="recommend-info">
                                            <div class="recommend-title">Dạng ra</div>
                                            <div class="recommend-stats">
                                                <span>2 chương</span>
                                                <span>980K</span>
                                            </div>
                                        </div>
                                    </div>
                                    <div class="recommend-card" data-title="Hoàn thành" data-rating="4.8" data-votes="2100">
                                        <div class="recommend-cover"></div>
                                        <div class="recommend-info">
                                            <div class="recommend-title">Hoàn thành</div>
                                            <div class="recommend-stats">
                                                <span>2 chương</span>
                                                <span>2.1M</span>
                                            </div>
                                        </div>
                                    </div>
                                    <div class="recommend-card" data-title="Hỏi Tài" data-rating="4.4" data-votes="1400">
                                        <div class="recommend-cover"></div>
                                        <div class="recommend-info">
                                            <div class="recommend-title">Hỏi Tài</div>
                                            <div class="recommend-stats">
                                                <span>1 chương</span>
                                                <span>1.4M</span>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- Right column (trong home-grid) -->
                        <div>
                            <div class="top-list">
                                <div class="section-header">
                                    <h2>Xem Nhiều Nhất</h2>
                                </div>
                                <div class="top-item" data-id="cosmic-legends" data-title="Cosmic Legends" data-rating="4.9" data-votes="2100">
                                    <span class="top-rank">1</span>
                                    <div class="top-info">
                                        <div class="top-title">Cosmic Legends</div>
                                        <div class="top-views">2100K</div>
                                    </div>
                                </div>
                                <div class="top-item" data-id="steel-resolve" data-title="Steel Resolve" data-rating="4.6" data-votes="1250">
                                    <span class="top-rank">2</span>
                                    <div class="top-info">
                                        <div class="top-title">Steel Resolve</div>
                                        <div class="top-views">1680K</div>
                                    </div>
                                </div>
                                <div class="top-item" data-id="dragon-quest" data-title="Dragon Quest" data-rating="4.7" data-votes="980">
                                    <span class="top-rank">3</span>
                                    <div class="top-info">
                                        <div class="top-title">Dragon Quest</div>
                                        <div class="top-views">1450K</div>
                                    </div>
                                </div>
                                <div class="top-item" data-id="shadow-warrior" data-title="Shadow Warrior" data-rating="4.8" data-votes="1250">
                                    <span class="top-rank">4</span>
                                    <div class="top-info">
                                        <div class="top-title">Shadow Warrior</div>
                                        <div class="top-views">1250K</div>
                                    </div>
                                </div>
                                <div class="top-item" data-id="mystic-dreams" data-title="Mystic Dreams" data-rating="4.3" data-votes="980">
                                    <span class="top-rank">5</span>
                                    <div class="top-info">
                                        <div class="top-title">Mystic Dreams</div>
                                        <div class="top-views">980K</div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Các section khác (ẩn mặc định) -->
                <div id="newSection" class="content-section hidden"><h2>Nội dung Mới cập nhật</h2></div>
                <div id="hotSection" class="content-section hidden"><h2>Nội dung Hot nhất</h2></div>
                <div id="genreSection" class="content-section hidden"><h2>Nội dung Thể loại</h2></div>
                <div id="rankSection" class="content-section hidden"><h2>Nội dung BXH</h2></div>
                <div id="completedSection" class="content-section hidden"><h2>Nội dung Hoàn thành</h2></div>
                <div id="groupsSection" class="content-section hidden"><h2>Nội dung Nhóm dịch</h2></div>
                <div id="profilePageSection" class="content-section hidden"><h2>Nội dung Cá nhân</h2></div>

                <!-- TRANG CHI TIẾT TRUYỆN -->
                <div id="detailSection" class="content-section hidden">
                    <div class="breadcrumb">
                        <a href="#" id="breadcrumbHome">Trang chủ</a> > <a href="#" id="breadcrumbCategory">Thể loại</a> > <span id="breadcrumbTitle">Đồ Đệ Của Ta Đều Là Đại Phản Phái</span>
                    </div>
                    <div class="detail-container">
                        <div class="detail-header">
                            <div class="detail-cover" id="detailCover">Ảnh bìa</div>
                            <div class="detail-info">
                                <h1 id="detailTitle">Đồ Đệ Của Ta Đều Là Đại Phản Phái</h1>
                                <div class="detail-meta">
                                    <div class="detail-meta-item"><span>Tác giả</span><strong id="detailAuthor">TruyenQQ, Mộng Tiên Giới</strong></div>
                                    <div class="detail-meta-item"><span>Tình trạng</span><strong id="detailStatus">Đang tiến hành</strong></div>
                                    <div class="detail-meta-item"><span>Thể loại</span><strong id="detailGenre">Adventure - Chuyển Sinh - Cổ Đại - Manhua - Truyện Màu - Xuyên Không</strong></div>
                                    <div class="detail-meta-item"><span>Lượt xem</span><strong id="detailViews">427,480</strong></div>
                                </div>
                                <div class="rating">
                                    <div class="stars-interactive">
                                        <i class="star fas fa-star" data-value="1"></i>
                                        <i class="star fas fa-star" data-value="2"></i>
                                        <i class="star fas fa-star" data-value="3"></i>
                                        <i class="star fas fa-star" data-value="4"></i>
                                        <i class="star fas fa-star" data-value="5"></i>
                                    </div>
                                    <span class="rating-text">5/5 - 605 Lượt đánh giá</span>
                                </div>
                                <div class="follow-info">
                                    <button class="follow-btn" id="followBtn">Theo dõi</button>
                                    <span><strong>155.492</strong> Người đã theo dõi</span>
                                </div>
                                <div class="action-buttons">
                                    <button class="action-btn" id="readFirstBtn">Đọc từ đầu</button>
                                    <button class="action-btn" id="readLatestBtn">Đọc mới nhất</button>
                                    <button class="action-btn secondary" id="readContinueBtn">Đọc tiếp ></button>
                                </div>
                            </div>
                        </div>
                        <div class="description" id="detailDescription">
                            <strong>NỘI DUNG TRUYỆN ĐỒ ĐỆ CỦA TA ĐỀU LÀ ĐẠI PHẢN PHÁI</strong><br>
                            Chào mừng bạn đến với NetTruyen – không gian đọc truyện tranh online hoàn hảo dành cho tất cả các fan truyện tranh!
                        </div>
                    </div>
                </div>

                <!-- TRANG ĐỌC TRUYỆN (READER) -->
                <div id="readerSection" class="content-section hidden">
                    <div class="breadcrumb">
                        <a href="#" id="readerBreadcrumbHome">Trang chủ</a> > <a href="#" id="readerBreadcrumbCategory">Thể loại</a> > <a href="#" id="readerBreadcrumbDetail">Đỗ Đệ Của Ta Đều Là Đại Phản Phái</a> > <span id="readerBreadcrumbChapter">Chapter 0</span>
                    </div>
                    <div class="reader-container">
                        <div class="reader-header">
                            <h1 class="reader-title" id="readerTitle">Đỗ Đệ Của Ta Đều Là Đại Phản Phái - Chapter 0</h1>
                            <div class="reader-update" id="readerUpdate">[Cập nhật lúc: 2024-02-29 00:56:44]</div>
                            <div class="server-notice">
                                <i class="fas fa-exclamation-triangle" style="color: #f97316;"></i> Nếu không xem được truyện vui lòng đổi "SERVER ẢNH" bên dưới
                            </div>
                            <div class="server-buttons">
                                <button class="server-btn active" id="server1">Server 1</button>
                                <button class="server-btn" id="server2">Server 2</button>
                                <button class="report-btn" id="reportError"><i class="fas fa-flag"></i> Báo lỗi</button>
                            </div>
                        </div>

                        <div class="reader-nav">
                            <div class="chapter-nav">
                                <span class="nav-arrow" id="prevChapter"><i class="fas fa-chevron-left"></i></span>
                                <select class="chapter-select" id="chapterSelect">
                                    <option value="0" selected>Chapter 0</option>
                                    <option value="1">Chapter 1</option>
                                    <option value="2">Chapter 2</option>
                                </select>
                                <span class="nav-arrow" id="nextChapter"><i class="fas fa-chevron-right"></i></span>
                            </div>
                            <div class="nav-hint">
                                Sử dụng mũi tên trái (←) hoặc phải (→) để chuyển chapter
                            </div>
                        </div>

                        <div class="reader-images">
                            <div class="reader-page">Trang 1 - Nội dung truyện sẽ hiển thị ở đây</div>
                            <div class="reader-page">Trang 2 - Cuộn dọc để đọc tiếp</div>
                            <div class="reader-page">Trang 3 - Ảnh truyện sẽ được load từ server</div>
                        </div>

                        <div class="ad-placeholder">
                            <i class="fas fa-ad"></i> Quảng cáo (Placeholder)
                        </div>
                    </div>
                </div>

                <!-- Xem thêm button -->
                <div style="text-align: center; margin: 30px 0;">
                    <a class="view-more-btn" id="viewMoreBtn">Xem thêm truyện <i class="fas fa-chevron-down"></i></a>
                </div>
            </div>

            <!-- Sidebar (lịch sử đọc, bảng xếp hạng, thể loại) -->
            <aside class="sidebar">
                <!-- Bảng xếp hạng với tabs -->
                <div class="sidebar-card">
                    <div class="sidebar-title"><i class="fas fa-crown" style="color: #fbbf24;"></i> Bảng xếp hạng</div>
                    <div class="rank-tabs">
                        <span class="rank-tab active" data-rank="day">Top ngày</span>
                        <span class="rank-tab" data-rank="week">Top tuần</span>
                        <span class="rank-tab" data-rank="month">Top tháng</span>
                    </div>
                    <div class="rank-content active" id="rankDay">
                        <div class="hot-item"><span class="hot-rank">1</span><div class="hot-info"><h4>Bách Luyện Thành Thần</h4><p>Chapter 1295 • 193K</p></div></div>
                        <div class="hot-item"><span class="hot-rank">2</span><div class="hot-info"><h4>Đại Phụng Đa Canh Nhân</h4><p>Chapter 619 • 297K</p></div></div>
                        <div class="hot-item"><span class="hot-rank">3</span><div class="hot-info"><h4>Tinh Giáp Hôn Tướng</h4><p>Chapter 349 • 305K</p></div></div>
                        <div class="hot-item"><span class="hot-rank">4</span><div class="hot-info"><h4>Tự Kỷ Luật Ta Đầy Bất Khả...</h4><p>Chapter 200 • 280K</p></div></div>
                        <div class="hot-item"><span class="hot-rank">5</span><div class="hot-info"><h4>Solo Leveling</h4><p>Chapter 200 • 1.2M</p></div></div>
                    </div>
                    <div class="rank-content" id="rankWeek">
                        <div class="hot-item"><span class="hot-rank">1</span><div class="hot-info"><h4>One Piece</h4><p>Chapter 1090 • 980K</p></div></div>
                        <div class="hot-item"><span class="hot-rank">2</span><div class="hot-info"><h4>Dớp Dâng</h4><p>Chapter 567 • 854K</p></div></div>
                        <div class="hot-item"><span class="hot-rank">3</span><div class="hot-info"><h4>Thiên mệnh đế chủ</h4><p>Chapter 420 • 620K</p></div></div>
                    </div>
                    <div class="rank-content" id="rankMonth">
                        <div class="hot-item"><span class="hot-rank">1</span><div class="hot-info"><h4>Solo Leveling</h4><p>Chapter 200 • 1.2M</p></div></div>
                        <div class="hot-item"><span class="hot-rank">2</span><div class="hot-info"><h4>One Piece</h4><p>Chapter 1090 • 980K</p></div></div>
                        <div class="hot-item"><span class="hot-rank">3</span><div class="hot-info"><h4>Dớp Dâng</h4><p>Chapter 567 • 854K</p></div></div>
                    </div>
                </div>

                <!-- Lịch sử đọc -->
                <div class="history-card">
                    <h3>Lịch sử đọc <span class="view-all-link" style="font-size: 13px;">Xem tất cả</span></h3>
                    <div class="history-item">
                        <div class="history-cover">Ảnh</div>
                        <div class="history-info">
                            <div class="history-title">Vạn Cố Chí Tôn</div>
                            <div class="history-meta">Đọc tiếp Chapter 502 ></div>
                        </div>
                        <span class="delete-btn"><i class="fas fa-times"></i></span>
                    </div>
                    <div class="history-item">
                        <div class="history-cover">Ảnh</div>
                        <div class="history-info">
                            <div class="history-title">Bỏ Làm Simp Chúa, Ta Có Trong...</div>
                            <div class="history-meta">Đọc tiếp Chapter 278 ></div>
                        </div>
                        <span class="delete-btn"><i class="fas fa-times"></i></span>
                    </div>
                    <div class="history-item">
                        <div class="history-cover">Ảnh</div>
                        <div class="history-info">
                            <div class="history-title">One Piece</div>
                            <div class="history-meta">Đọc tiếp Chapter 2 ></div>
                        </div>
                        <span class="delete-btn"><i class="fas fa-times"></i></span>
                    </div>
                </div>

                <!-- Thể loại -->
                <div class="sidebar-card">
                    <div class="sidebar-title"><i class="fas fa-tags"></i> Thể loại</div>
                    <div class="genre-tags">
                        <span class="genre-tag">Hành động</span>
                        <span class="genre-tag">Hài hước</span>
                        <span class="genre-tag">Ngôn tình</span>
                        <span class="genre-tag">Đam mỹ</span>
                        <span class="genre-tag">Xuyên không</span>
                        <span class="genre-tag">Kiếm hiệp</span>
                        <span class="genre-tag">Khoa huyễn</span>
                        <span class="genre-tag">Trọng sinh</span>
                    </div>
                </div>
            </aside>
        </div>

        <!-- PHẦN MUA TRUYỆN -->
        <div class="purchase-section">
            <h2><i class="fas fa-gem" style="color: #f97316;"></i> Mua chương / Ủng hộ truyện</h2>
            <div class="purchase-options">
                <div class="purchase-card"><i class="fas fa-coins" style="font-size: 32px; color: #fbbf24;"></i><div class="price">10 Xu</div><p>Mua chương 126</p><button class="btn-buy" id="buyChapter">Mua ngay</button></div>
                <div class="purchase-card"><i class="fas fa-gem" style="font-size: 32px; color: #2563eb;"></i><div class="price">50 Xu</div><p>Ủng hộ tác giả</p><button class="btn-buy" id="donate">Ủng hộ</button></div>
                <div class="purchase-card"><i class="fas fa-crown" style="font-size: 32px; color: #f97316;"></i><div class="price">Gói VIP</div><p>Đọc không giới hạn</p><button class="btn-buy" id="vip">Đăng ký</button></div>
            </div>
        </div>

        <!-- CHATBOX AI (đã sửa) -->
        <div class="chatbox" id="chatbox">
            <div class="chat-header" id="toggleChat">
                <h3><i class="fas fa-robot" style="color: #f97316;"></i> Trợ lý AI</h3>
                <i class="fas fa-minus" id="chatIcon"></i>
            </div>
            <div class="chat-body" id="chatBody">
                <div class="message ai">Xin chào! Tôi là trợ lý đọc truyện. Bạn cần tìm gì?</div>
            </div>
            <div class="chat-footer">
                <input type="text" id="chatInput" placeholder="Nhập tin nhắn...">
                <button id="sendChat"><i class="fas fa-paper-plane"></i></button>
            </div>
        </div>
        @RenderBody()
    </div>

    <script>
        // ===== BIẾN GIẢ LẬP ĐĂNG NHẬP =====
        let isLoggedIn = false;

        // ===== MODAL AUTH =====
        const authModal = document.getElementById('authModal');
        const profileModal = document.getElementById('profileModal');
        const loginBtn = document.getElementById('loginBtn');
        const avatarBtn = document.getElementById('avatarBtn');
        const closeModal = document.getElementById('closeModal');
        const closeProfileModal = document.getElementById('closeProfileModal');
        const loginForm = document.getElementById('loginForm');
        const registerForm = document.getElementById('registerForm');
        const confirmEmailMsg = document.getElementById('confirmEmailMessage');
        const showRegister = document.getElementById('showRegister');
        const showLogin = document.getElementById('showLogin');
        const backToLogin = document.getElementById('backToLogin');
        const modalTitle = document.getElementById('modalTitle');

        function openAuthModal() { authModal.classList.add('active'); resetToLogin(); }
        function openProfileModal() { profileModal.classList.add('active'); }
        function closeAllModals() { authModal.classList.remove('active'); profileModal.classList.remove('active'); }

        avatarBtn.addEventListener('click', () => { if (isLoggedIn) openProfileModal(); else openAuthModal(); });
        loginBtn.addEventListener('click', (e) => { e.preventDefault(); if (isLoggedIn) openProfileModal(); else openAuthModal(); });
        closeModal.addEventListener('click', closeAllModals);
        closeProfileModal.addEventListener('click', closeAllModals);
        window.addEventListener('click', (e) => { if (e.target === authModal) closeAllModals(); if (e.target === profileModal) closeAllModals(); });

        function resetToLogin() { loginForm.style.display = 'block'; registerForm.style.display = 'none'; confirmEmailMsg.style.display = 'none'; modalTitle.innerText = 'Đăng nhập'; }
        showRegister.addEventListener('click', () => { loginForm.style.display = 'none'; registerForm.style.display = 'block'; confirmEmailMsg.style.display = 'none'; modalTitle.innerText = 'Đăng ký'; });
        showLogin.addEventListener('click', resetToLogin);
        backToLogin.addEventListener('click', resetToLogin);

        // ===== CAPTCHA =====
        function randomCaptcha() { let chars = 'ABCDEFGHJKLMNPQRSTUVWXYZ0123456789', result = ''; for (let i = 0; i < 5; i++) result += chars[Math.floor(Math.random() * chars.length)]; return result; }
        document.getElementById('refreshCaptcha').addEventListener('click', function() { document.getElementById('captchaText').innerText = randomCaptcha(); });
        document.getElementById('refreshRegCaptcha').addEventListener('click', function() { document.getElementById('regCaptchaText').innerText = randomCaptcha(); });

        // ===== GIẢ LẬP ĐĂNG NHẬP =====
        document.getElementById('doLogin').addEventListener('click', function() {
            const captcha = document.getElementById('captchaText').innerText;
            const input = document.getElementById('captchaInput').value;
            if (input.toUpperCase() !== captcha) { alert('Mã captcha không đúng!'); return; }
            alert('Đăng nhập thành công! (Mô phỏng)');
            isLoggedIn = true;
            authModal.classList.remove('active');
            loginBtn.innerHTML = '<i class="far fa-user"></i> <span>Tài khoản</span>';
        });

        document.getElementById('doRegister').addEventListener('click', function() {
            const captcha = document.getElementById('regCaptchaText').innerText;
            const input = document.getElementById('regCaptchaInput').value;
            if (input.toUpperCase() !== captcha) { alert('Mã captcha không đúng!'); return; }
            const email = document.getElementById('regEmail').value;
            if (!email) { alert('Vui lòng nhập email'); return; }
            loginForm.style.display = 'none'; registerForm.style.display = 'none'; confirmEmailMsg.style.display = 'block';
            modalTitle.innerText = 'Xác nhận email';
            document.querySelector('#confirmEmailMessage strong').innerText = email || 'user@example.com';
        });

        // ===== ĐĂNG XUẤT (modal) =====
        document.getElementById('logoutBtn').addEventListener('click', function() {
            isLoggedIn = false; profileModal.classList.remove('active'); loginBtn.innerHTML = '<i class="far fa-user"></i> <span>Đăng nhập</span>'; alert('Đã đăng xuất');
        });

        // ===== PROFILE TABS (modal) =====
        const tabs = document.querySelectorAll('.profile-tab');
        const panes = document.querySelectorAll('.profile-pane');
        tabs.forEach(tab => {
            tab.addEventListener('click', () => {
                if (!tab.dataset.tab) return;
                tabs.forEach(t => t.classList.remove('active'));
                tab.classList.add('active');
                panes.forEach(pane => pane.classList.remove('active'));
                document.getElementById(`pane-${tab.dataset.tab}`).classList.add('active');
            });
        });

        // ===== MOBILE MENU =====
        const mobileMenuBtn = document.getElementById('mobileMenuBtn');
        const mobileMenu = document.getElementById('mobileMenu');
        const mobileMenuOverlay = document.getElementById('mobileMenuOverlay');
        const closeMenu = document.getElementById('closeMenu');
        function openMobileMenu() { mobileMenu.classList.add('open'); mobileMenuOverlay.classList.add('active'); }
        function closeMobileMenu() { mobileMenu.classList.remove('open'); mobileMenuOverlay.classList.remove('active'); }
        mobileMenuBtn.addEventListener('click', openMobileMenu);
        closeMenu.addEventListener('click', closeMobileMenu);
        mobileMenuOverlay.addEventListener('click', closeMobileMenu);

        // ===== CÁC SECTION =====
        const homeSection = document.getElementById('homeSection');
        const newSection = document.getElementById('newSection');
        const hotSection = document.getElementById('hotSection');
        const genreSection = document.getElementById('genreSection');
        const rankSection = document.getElementById('rankSection');
        const completedSection = document.getElementById('completedSection');
        const groupsSection = document.getElementById('groupsSection');
        const profilePageSection = document.getElementById('profilePageSection');
        const detailSection = document.getElementById('detailSection');
        const readerSection = document.getElementById('readerSection');

        const tabHome = document.getElementById('tabHome');
        const tabNew = document.getElementById('tabNew');
        const tabHot = document.getElementById('tabHot');
        const tabGenre = document.getElementById('tabGenre');
        const tabRank = document.getElementById('tabRank');
        const tabCompleted = document.getElementById('tabCompleted');
        const tabGroups = document.getElementById('tabGroups');
        const tabProfilePage = document.getElementById('tabProfilePage');

        function hideAllSections() {
            homeSection.classList.add('hidden');
            newSection.classList.add('hidden');
            hotSection.classList.add('hidden');
            genreSection.classList.add('hidden');
            rankSection.classList.add('hidden');
            completedSection.classList.add('hidden');
            groupsSection.classList.add('hidden');
            profilePageSection.classList.add('hidden');
            detailSection.classList.add('hidden');
            readerSection.classList.add('hidden');
        }

        function removeActiveTabs() {
            tabHome.classList.remove('active');
            tabNew.classList.remove('active');
            tabHot.classList.remove('active');
            tabGenre.classList.remove('active');
            tabRank.classList.remove('active');
            tabCompleted.classList.remove('active');
            tabGroups.classList.remove('active');
            tabProfilePage.classList.remove('active');
        }

        function showHome() { hideAllSections(); homeSection.classList.remove('hidden'); removeActiveTabs(); tabHome.classList.add('active'); }
        function showNew() { hideAllSections(); newSection.classList.remove('hidden'); removeActiveTabs(); tabNew.classList.add('active'); }
        function showHot() { hideAllSections(); hotSection.classList.remove('hidden'); removeActiveTabs(); tabHot.classList.add('active'); }
        function showGenre() { hideAllSections(); genreSection.classList.remove('hidden'); removeActiveTabs(); tabGenre.classList.add('active'); }
        function showRank() { hideAllSections(); rankSection.classList.remove('hidden'); removeActiveTabs(); tabRank.classList.add('active'); }
        function showCompleted() { hideAllSections(); completedSection.classList.remove('hidden'); removeActiveTabs(); tabCompleted.classList.add('active'); }
        function showGroups() { hideAllSections(); groupsSection.classList.remove('hidden'); removeActiveTabs(); tabGroups.classList.add('active'); }
        function showProfilePage() { hideAllSections(); profilePageSection.classList.remove('hidden'); removeActiveTabs(); tabProfilePage.classList.add('active'); }
        function showDetail() { hideAllSections(); detailSection.classList.remove('hidden'); removeActiveTabs(); }
        function showReader() { hideAllSections(); readerSection.classList.remove('hidden'); removeActiveTabs(); }

        // Mặc định hiện trang chủ
        showHome();

        tabHome.addEventListener('click', (e) => { e.preventDefault(); showHome(); });
        tabNew.addEventListener('click', (e) => { e.preventDefault(); showNew(); });
        tabHot.addEventListener('click', (e) => { e.preventDefault(); showHot(); });
        tabGenre.addEventListener('click', (e) => { e.preventDefault(); showGenre(); });
        tabRank.addEventListener('click', (e) => { e.preventDefault(); showRank(); });
        tabCompleted.addEventListener('click', (e) => { e.preventDefault(); showCompleted(); });
        tabGroups.addEventListener('click', (e) => { e.preventDefault(); showGroups(); });
        tabProfilePage.addEventListener('click', (e) => { e.preventDefault(); showProfilePage(); });

        // Mobile menu items
        document.getElementById('mobileHome').addEventListener('click', (e) => { e.preventDefault(); showHome(); closeMobileMenu(); });
        document.getElementById('mobileNew').addEventListener('click', (e) => { e.preventDefault(); showNew(); closeMobileMenu(); });
        document.getElementById('mobileHot').addEventListener('click', (e) => { e.preventDefault(); showHot(); closeMobileMenu(); });
        document.getElementById('mobileGenre').addEventListener('click', (e) => { e.preventDefault(); showGenre(); closeMobileMenu(); });
        document.getElementById('mobileRank').addEventListener('click', (e) => { e.preventDefault(); showRank(); closeMobileMenu(); });
        document.getElementById('mobileCompleted').addEventListener('click', (e) => { e.preventDefault(); showCompleted(); closeMobileMenu(); });
        document.getElementById('mobileGroups').addEventListener('click', (e) => { e.preventDefault(); showGroups(); closeMobileMenu(); });
        document.getElementById('mobileProfilePage').addEventListener('click', (e) => { e.preventDefault(); if (isLoggedIn) showProfilePage(); else openAuthModal(); closeMobileMenu(); });

        // ===== SIDEBAR RANK TABS =====
        const rankTabs = document.querySelectorAll('.rank-tab');
        const rankContents = document.querySelectorAll('.rank-content');
        rankTabs.forEach(tab => {
            tab.addEventListener('click', () => {
                rankTabs.forEach(t => t.classList.remove('active'));
                tab.classList.add('active');
                rankContents.forEach(content => content.classList.remove('active'));
                document.getElementById('rank' + tab.dataset.rank.charAt(0).toUpperCase() + tab.dataset.rank.slice(1)).classList.add('active');
            });
        });

        // ===== CHATBOX TOGGLE (thu nhỏ thành bong bóng) =====
        const chatbox = document.getElementById('chatbox');
        const toggleChat = document.getElementById('toggleChat');
        const chatIcon = document.getElementById('chatIcon');
        const chatBody = document.getElementById('chatBody');
        const chatInput = document.getElementById('chatInput');
        const sendBtn = document.getElementById('sendChat');

        toggleChat.addEventListener('click', () => {
            chatbox.classList.toggle('closed');
            if (chatbox.classList.contains('closed')) {
                chatIcon.className = 'fas fa-comment'; // khi thu nhỏ hiện icon chat
            } else {
                chatIcon.className = 'fas fa-minus'; // khi mở hiện dấu trừ
            }
        });

        // Chat functions
        function addMessage(text, sender) {
            const msgDiv = document.createElement('div');
            msgDiv.classList.add('message', sender);
            msgDiv.innerText = text;
            chatBody.appendChild(msgDiv);
            chatBody.scrollTop = chatBody.scrollHeight;
        }
        function sendMessage() {
            const text = chatInput.value.trim();
            if (!text) return;
            addMessage(text, 'user');
            chatInput.value = '';
            setTimeout(() => {
                let reply = 'Tôi là AI, tôi có thể gợi ý truyện hot hoặc tìm kiếm. Bạn muốn gì?';
                if (text.toLowerCase().includes('truyện hot') || text.toLowerCase().includes('đề xuất')) {
                    reply = 'Gợi ý cho bạn: "Đại chiến thần", "Solo Leveling", "One Piece" đang rất hot!';
                } else if (text.includes('cảm ơn')) {
                    reply = 'Không có gì! Đọc truyện vui vẻ nhé.';
                }
                addMessage(reply, 'ai');
            }, 600);
        }
        sendBtn.addEventListener('click', sendMessage);
        chatInput.addEventListener('keypress', (e) => {
            if (e.key === 'Enter') sendMessage();
        });

        // ===== CÁC BUTTON TƯƠNG TÁC =====
        document.getElementById('searchBtn').addEventListener('click', function() { alert('Bạn đang tìm: "' + document.getElementById('searchInput').value + '" (Mô phỏng tìm kiếm)'); });
        document.getElementById('logo').addEventListener('click', function(e) { e.preventDefault(); showHome(); });
        document.getElementById('notificationBtn').addEventListener('click', function(e) { e.preventDefault(); alert('Bạn có 3 thông báo mới (mô phỏng)'); });
        document.getElementById('readFeaturedBtn').addEventListener('click', function() { fillDetailAndShow(this); });
        document.getElementById('viewAllUpdates').addEventListener('click', function() { alert('Xem tất cả truyện mới cập nhật'); });
        document.getElementById('viewMoreBtn').addEventListener('click', function() { alert('Tải thêm truyện (mô phỏng)'); });
        document.getElementById('buyChapter').addEventListener('click', () => alert('Mô phỏng: Bạn đã mua chương 126 bằng 10 xu.'));
        document.getElementById('donate').addEventListener('click', () => alert('Cảm ơn bạn đã ủng hộ! (Mô phỏng)'));
        document.getElementById('vip').addEventListener('click', () => alert('Đăng ký gói VIP thành công! (Mô phỏng)'));

        // ===== XỬ LÝ RATING (SAO) =====
        const stars = document.querySelectorAll('.star');
        const ratingText = document.querySelector('.rating-text');
        let currentRating = 5;
        let totalVotes = 605;

        function updateRatingDisplay(rating) {
            stars.forEach((star, index) => {
                if (index < rating) {
                    star.classList.add('selected');
                } else {
                    star.classList.remove('selected');
                }
            });
            ratingText.innerText = `${rating}/5 - ${totalVotes} Lượt đánh giá`;
        }
        updateRatingDisplay(currentRating);

        stars.forEach(star => {
            star.addEventListener('click', function() {
                const value = parseInt(this.dataset.value);
                currentRating = value;
                totalVotes++;
                updateRatingDisplay(value);
                alert(`Bạn đã đánh giá ${value} sao! Cảm ơn bạn.`);
            });
        });

        // ===== CLICK CARD TRUYỆN -> HIỂN THỊ CHI TIẾT =====
        function fillDetailAndShow(card) {
            const title = card.dataset.title || 'Đồ Đệ Của Ta Đều Là Đại Phản Phái';
            const author = card.dataset.author || 'TruyenQQ, Mộng Tiên Giới';
            const genre = card.dataset.genre || 'Adventure - Chuyển Sinh - Cổ Đại - Manhua - Truyện Màu - Xuyên Không';
            const views = card.dataset.views || '427,480';
            const rating = parseFloat(card.dataset.rating) || 5.0;
            const votes = parseInt(card.dataset.votes) || 605;
            const description = card.dataset.description || '';
            const cover = card.dataset.cover || '';
            const status = card.dataset.status || 'Đang tiến hành';

            document.getElementById('detailTitle').innerText = title;
            document.getElementById('detailAuthor').innerText = author;
            document.getElementById('detailGenre').innerText = genre;
            document.getElementById('detailViews').innerText = views;
            document.getElementById('detailStatus').innerText = status;
            document.getElementById('breadcrumbTitle').innerText = title;

            const detailDescription = document.getElementById('detailDescription');
            detailDescription.innerHTML = description
                ? `<strong>NỘI DUNG TRUYỆN ${title.toUpperCase()}</strong><br>${description}`
                : '<strong>NỘI DUNG TRUYỆN ĐỒ ĐỆ CỦA TA ĐỀU LÀ ĐẠI PHẢN PHÁI</strong><br>Chào mừng bạn đến với NetTruyen – không gian đọc truyện tranh online hoàn hảo dành cho tất cả các fan truyện tranh!';

            const detailCover = document.getElementById('detailCover');
            if (cover) {
                detailCover.style.backgroundImage = `url('${cover}')`;
                detailCover.style.backgroundSize = 'cover';
                detailCover.style.backgroundPosition = 'center';
                detailCover.innerText = '';
            } else {
                detailCover.style.backgroundImage = '';
                detailCover.innerText = 'Ảnh bìa';
            }

            currentRating = Math.round(rating);
            totalVotes = votes;
            updateRatingDisplay(currentRating);

            showDetail();
        }

        document.addEventListener('click', (event) => {
            const card = event.target.closest('.update-item, .top-item, .recommend-card');
            if (card) {
                fillDetailAndShow(card);
            }
        });

        async function loadStories() {
            try {
                const response = await fetch('/api/stories');
                if (!response.ok) {
                    throw new Error('Không tải được danh sách truyện');
                }
                const stories = await response.json();
                if (!Array.isArray(stories) || stories.length === 0) {
                    return;
                }

                renderFeaturedStory(stories[0]);
                renderUpdateStories(stories);
                renderTopStories(stories);
                renderRecommendStories(stories);
            } catch (error) {
                console.error(error);
            }
        }

        function buildStoryData(story) {
            const title = story.title || story.Title || 'Chưa có tiêu đề';
            return {
                id: story.id || story.Id,
                title,
                description: story.description || story.Description || '',
                author: story.authorName || story.AuthorName || 'Đang cập nhật',
                genre: story.categoryName || story.CategoryName || 'Đang cập nhật',
                status: story.status || story.Status || 'Đang tiến hành',
                cover: story.coverImage || story.CoverImage || '',
                views: story.views || story.Views || '0',
                rating: story.rating || story.Rating || '5',
                votes: story.votes || story.Votes || '0'
            };
        }

        function setStoryDataset(element, storyData) {
            element.dataset.id = storyData.id || '';
            element.dataset.title = storyData.title;
            element.dataset.author = storyData.author;
            element.dataset.genre = storyData.genre;
            element.dataset.views = storyData.views;
            element.dataset.rating = storyData.rating;
            element.dataset.votes = storyData.votes;
            element.dataset.description = storyData.description;
            element.dataset.cover = storyData.cover;
            element.dataset.status = storyData.status;
        }

        function renderFeaturedStory(story) {
            const storyData = buildStoryData(story);
            document.querySelector('.featured-large-content h2').innerText = storyData.title;
            document.querySelector('.featured-large-content p').innerText = storyData.description || 'Đang cập nhật nội dung.';
            const stats = document.querySelectorAll('.featured-stats span');
            if (stats.length >= 2) {
                stats[1].innerHTML = `<i class="fas fa-eye"></i> ${storyData.views} lượt xem`;
            }

            const featuredCover = document.querySelector('.featured-large-cover');
            if (storyData.cover) {
                featuredCover.style.backgroundImage = `url('${storyData.cover}')`;
                featuredCover.style.backgroundSize = 'cover';
                featuredCover.style.backgroundPosition = 'center';
                featuredCover.innerText = '';
            } else {
                featuredCover.style.backgroundImage = '';
                featuredCover.innerText = 'Ảnh bìa';
            }

            const featuredButton = document.getElementById('readFeaturedBtn');
            setStoryDataset(featuredButton, storyData);
        }

        function renderUpdateStories(stories) {
            const list = document.querySelector('.update-list');
            list.querySelectorAll('.update-item').forEach(item => item.remove());
            stories.slice(0, 5).forEach(story => {
                const storyData = buildStoryData(story);
                const item = document.createElement('div');
                item.className = 'update-item';
                setStoryDataset(item, storyData);
                item.innerHTML = `
                    <div class="update-cover" style="${storyData.cover ? `background-image:url('${storyData.cover}')` : ''}"></div>
                    <div class="update-info">
                        <div class="update-title">${storyData.title}</div>
                        <div class="update-chapter">Cập nhật gần đây</div>
                        <div class="update-meta">
                            <span><i class="fas fa-star" style="color: #fbbf24;"></i> ${storyData.rating}</span>
                            <span><i class="fas fa-eye"></i> ${storyData.views}</span>
                        </div>
                    </div>
                `;
                list.appendChild(item);
            });
        }

        function renderTopStories(stories) {
            const list = document.querySelector('.top-list');
            list.querySelectorAll('.top-item').forEach(item => item.remove());
            stories.slice(0, 5).forEach((story, index) => {
                const storyData = buildStoryData(story);
                const item = document.createElement('div');
                item.className = 'top-item';
                setStoryDataset(item, storyData);
                item.innerHTML = `
                    <span class="top-rank">${index + 1}</span>
                    <div class="top-info">
                        <div class="top-title">${storyData.title}</div>
                        <div class="top-views">${storyData.views}</div>
                    </div>
                `;
                list.appendChild(item);
            });
        }

        function renderRecommendStories(stories) {
            const grid = document.getElementById('recommendAll');
            grid.querySelectorAll('.recommend-card').forEach(card => card.remove());
            stories.slice(0, 4).forEach(story => {
                const storyData = buildStoryData(story);
                const card = document.createElement('div');
                card.className = 'recommend-card';
                setStoryDataset(card, storyData);
                card.innerHTML = `
                    <div class="recommend-cover" style="${storyData.cover ? `background-image:url('${storyData.cover}')` : ''}"></div>
                    <div class="recommend-info">
                        <div class="recommend-title">${storyData.title}</div>
                        <div class="recommend-stats">
                            <span>${storyData.status}</span>
                            <span>${storyData.views}</span>
                        </div>
                    </div>
                `;
                grid.appendChild(card);
            });
        }

        // Các nút xóa trong lịch sử
        document.querySelectorAll('.delete-btn').forEach(btn => {
            btn.addEventListener('click', (e) => { e.stopPropagation(); alert('Đã xóa lịch sử'); });
        });

        // Recommend tabs
        const recommendTabs = document.querySelectorAll('.recommend-tab');
        recommendTabs.forEach(tab => {
            tab.addEventListener('click', () => {
                recommendTabs.forEach(t => t.classList.remove('active'));
                tab.classList.add('active');
                alert('Chuyển tab ' + tab.innerText);
            });
        });

        // Nút chi tiết trong trang detail
        document.getElementById('followBtn').addEventListener('click', () => alert('Đã theo dõi truyện (mô phỏng)'));
        document.getElementById('readFirstBtn').addEventListener('click', () => { alert('Đọc từ đầu'); showReader(); });
        document.getElementById('readLatestBtn').addEventListener('click', () => { alert('Đọc mới nhất'); showReader(); });
        document.getElementById('readContinueBtn').addEventListener('click', () => { alert('Đọc tiếp'); showReader(); });
        document.getElementById('breadcrumbHome').addEventListener('click', (e) => { e.preventDefault(); showHome(); });
        document.getElementById('breadcrumbCategory').addEventListener('click', (e) => { e.preventDefault(); showGenre(); });

        loadStories();

        // ===== READER PAGE INTERACTIONS =====
        // Breadcrumb
        document.getElementById('readerBreadcrumbHome').addEventListener('click', (e) => { e.preventDefault(); showHome(); });
        document.getElementById('readerBreadcrumbCategory').addEventListener('click', (e) => { e.preventDefault(); showGenre(); });
        document.getElementById('readerBreadcrumbDetail').addEventListener('click', (e) => { e.preventDefault(); showDetail(); });

        // Server buttons
        document.getElementById('server1').addEventListener('click', function() {
            document.getElementById('server1').classList.add('active');
            document.getElementById('server2').classList.remove('active');
            alert('Chuyển sang Server 1');
        });
        document.getElementById('server2').addEventListener('click', function() {
            document.getElementById('server2').classList.add('active');
            document.getElementById('server1').classList.remove('active');
            alert('Chuyển sang Server 2');
        });
        document.getElementById('reportError').addEventListener('click', () => alert('Báo lỗi thành công (mô phỏng)'));

        // Chapter navigation
        document.getElementById('prevChapter').addEventListener('click', () => alert('Chuyển đến chapter trước'));
        document.getElementById('nextChapter').addEventListener('click', () => alert('Chuyển đến chapter sau'));
        document.getElementById('chapterSelect').addEventListener('change', (e) => alert('Chuyển đến ' + e.target.value));

        // Keyboard navigation hint (giả lập)
        window.addEventListener('keydown', (e) => {
            if (e.key === 'ArrowLeft') {
                alert('← Chuyển chapter trước (mô phỏng)');
            } else if (e.key === 'ArrowRight') {
                alert('→ Chuyển chapter sau (mô phỏng)');
            }
        });
    </script>
</body>
</html>