@* @model WibuHub.ApplicationCore.Entities.Story *@
@model WibuHub.MVC.ViewModels.StoryVM
﻿

@{
    ViewData["Title"] = "Add New Story";
    bool isCreate = Model == null || Model.Id == Guid.Empty;
    string actionString = isCreate ? "Create" : "Edit";
    string buttonString = isCreate ? "Create" : "Update";
    }

    <div class="container mt-4">
        <h1>Add New Stories</h1>
        <hr />
        <div class="row">
            <div class="col-md-4">
                <form asp-action="@actionString" enctype="multipart/form-data">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    @if (!isCreate)
                    {
                        <input asp-for="Id" type="hidden" />
                    }
                    <div class="form-group mb-3">
                        <label asp-for="Title" class="control-label fw-bold">Story Title</label>
                        <input asp-for="Title" class="form-control" placeholder="Enter story title..." autofocus />
                        <span asp-validation-for="Title" class="text-danger"></span>
                    </div>
                    <div class="form-group mb-3">
                        <label asp-for="AlternativeName" class="control-label fw-bold">Alternative Name</label>
                        <input asp-for="AlternativeName" class="form-control" placeholder="English name, original name..." />
                        <span asp-validation-for="AlternativeName" class="text-danger"></span>
                    </div>
                <div class="form-group mb-3">
                    <label asp-for="Description" class="control-label fw-bold">Description</label>
                    <textarea asp-for="Description" class="form-control" rows="3" placeholder="Brief description of the story..."></textarea>
                    <span asp-validation-for="Description" class="text-danger"></span>
                </div>
                <div class="form-group mb-3">
                    <label asp-for="Slug" class="control-label fw-bold">URL Path (Slug)</label>
                    <input asp-for="Slug" class="form-control" placeholder="auto-generated-from-title" id="txtSlug" />
                    <small class="form-text text-muted">This is the URL path (no accents, hyphens).</small>
                    <span asp-validation-for="Slug" class="text-danger"></span>
                </div>
                <div class="form-group mb-3">
                    <label asp-for="Price" class="control-label fw-bold">Price</label>
                    <input asp-for="Price" asp-format="{0:0.####}" class="form-control" type="number" step="0.0001" min="0" />
                    <span asp-validation-for="Price" class="text-danger"></span>
                </div>
                <div class="form-group mb-3">
                    <label asp-for="Discount" class="control-label fw-bold">Discount</label>
                    <input asp-for="Discount" asp-format="{0:0.####}" class="form-control" type="number" step="0.0001" min="0" />
                    <span asp-validation-for="Discount" class="text-danger"></span>
                </div>
                <div class="form-group mb-3">
                    <label asp-for="CoverImageFile" class="control-label fw-bold">Cover Image</label>
                    <input asp-for="CoverImageFile" class="form-control" type="file" accept="image/*" />
                    <span asp-validation-for="CoverImageFile" class="text-danger"></span>
                    @if (!string.IsNullOrWhiteSpace(Model?.CoverImage))
                    {
                        <div class="mt-2">
                            <img src="@Model.CoverImage" alt="cover" class="img-thumbnail" style="max-width: 200px;" />
                        </div>
                    }
                </div>
                <div class="form-group mb-3">
                    <label asp-for="Status" class="control-label fw-bold">Status</label>
                    <select asp-for="Status" class="form-control">
                        <option value="0">In Progress</option>
                        <option value="1">Completed</option>
                        <option value="2">Paused</option>
                    </select>
                    <span asp-validation-for="Status" class="text-danger"></span>
                </div>
                <div class="form-group mb-3">
                    <label asp-for="AuthorId" class="control-label fw-bold">Author</label>
                    <select asp-for="AuthorId" class="form-control" asp-items="ViewBag.AuthorId"></select>
                    <span asp-validation-for="AuthorId" class="text-danger"></span>
                </div>
                <div class="form-group mb-3">
                    <label asp-for="CategoryIds" class="control-label fw-bold">Category</label>
                    <select asp-for="CategoryIds" class="form-control" asp-items="ViewBag.CategoryId" multiple size="6"></select>
                    <span asp-validation-for="CategoryIds" class="text-danger"></span>
                </div>
                <div class="form-group" style="text-align: center; margin-top: 10px;">
                    <input type="submit" value="@buttonString" class="btn btn-primary" />
                </div>
            </form>
        </div>
        <div id="divStoryList" class="col-md-8">
            @await Component.InvokeAsync("StoryList")
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    <script>
        // Tự động tạo Slug khi gõ Tiêu đề
        document.getElementById("Title").addEventListener("keyup", function () {
            let name = this.value;
            let slug = generateSlug(name);
            document.getElementById("txtSlug").value = slug;
        });
            function generateSlug(text) {
                return text.toString().toLowerCase()
                    .replace(/á|à|ả|ạ|ã|ă|ắ|ằ|ẳ|ặ|ẵ|â|ấ|ầ|ẩ|ậ|ẫ/g, 'a')
                    .replace(/é|è|ẻ|ẹ|ẽ|ê|ế|ề|ể|ệ|ễ/g, 'e')
                    .replace(/i|í|ì|ỉ|ị|ĩ/g, 'i')
                    .replace(/ó|ò|ỏ|ọ|õ|ô|ố|ồ|ổ|ộ|ỗ|ơ|ớ|ờ|ở|ợ|ỡ/g, 'o')
                    .replace(/ú|ù|ủ|ụ|ũ|ư|ứ|ừ|ử|ự|ữ/g, 'u')
                    .replace(/ý|ỳ|ỷ|ỵ|ỹ/g, 'y')
                    .replace(/đ/g, 'd')
                    .replace(/\s+/g, '-')
                    .replace(/[^\w\-]+/g, '')
                    .replace(/\-\-+/g, '-')
                    .replace(/^-+/, '')
                    .replace(/-+$/, '');
            }
        </script>
        <script>
            function openModalDeleteStory(idStory) {
                if (!confirm("Are you sure you want to delete this story?")) {
                    return;
                }
                deleteStory(idStory);
            }
            function deleteStory(idStory) {
                let inputToken = document.querySelector("input[name='__RequestVerificationToken']");
                if (!inputToken) {
                    console.error("Security token not found!");
                    return;
                }
                $.ajax({
                    url: '/Stories/Delete',
                    type: 'POST',
                    data: {
                        id: idStory,
                        __RequestVerificationToken: inputToken.value
                    },
                    success: function (response) {
                        if (response.isOK) {
                            refreshStoryList();
                        } else {
                            alert("Delete failed!");
                        }
                    },
                    error: function () {
                        alert("An error occurred while connecting to the server.");
                    }
                });
            }
            function refreshStoryList(page = 1) {
                $.get('/Stories/Reload', { page: page }, function (data) {
                    $('#divStoryList').html(data);
                });
            }
        </script>
}